# WordPress 6.9.1 — Локальная среда разработки

Локальная установка WordPress для разработки и тестирования.

## 📋 Требования

- **OpenServer Panel 6.x** или **Laragon 5+**
- **PHP 8.1+** (рекомендуется 8.4)
- **MySQL 5.7+** или **MariaDB 10.3+**

## 🚀 Быстрый старт

### OpenServer

1. Убедитесь, что проект активирован в панели OpenServer
2. Создайте базу данных `wordpress` через phpMyAdmin
3. Откройте `https://wordpress-6.9.1.local`
4. Пройдите мастер установки WordPress

### Laragon

1. Скопируйте файлы в `C:\laragon\www\wordpress-6.9.1.local`
2. Создайте базу данных `wordpress`
3. Откройте `http://wordpress-6.9.1.local`
4. Пройдите мастер установки WordPress

## 📁 Структура проекта

```
wordpress-6.9.1.local/
├── wp-admin/          # Панель администратора
├── wp-content/        # Контент (темы, плагины, загрузки)
│   ├── plugins/       # Плагины
│   ├── themes/        # Темы
│   └── uploads/       # Загруженные файлы
├── wp-includes/       # Ядро WordPress
├── wp-config.php      # Конфигурация
├── docs/              # Документация по настройке
│   ├── openserver-setup.md
│   └── laragon-setup.md
└── README.md          # Этот файл
```

## ⚙️ Конфигурация БД (по умолчанию)

| Параметр | Значение |
|----------|----------|
| Хост | `localhost` |
| БД | `wordpress` |
| Пользователь | `root` |
| Пароль | (пустой) |

## 📚 Документация

- [Настройка на OpenServer](docs/openserver-setup.md)
- [Настройка на Laragon](docs/laragon-setup.md)

## 🔧 Разработка

### Включение отладки

В файле `wp-config.php`:

```php
define( 'WP_DEBUG', true );
define( 'WP_DEBUG_LOG', true );
define( 'WP_DEBUG_DISPLAY', false );
```

Логи сохраняются в `wp-content/debug.log`

### Установка плагинов

```bash
# Через WP-CLI
wp plugin install <plugin-name> --activate

# Или через админ-панель: Плагины → Добавить новый
```

### Установка тем

```bash
# Через WP-CLI
wp theme install <theme-name> --activate

# Или через админ-панель: Внешний вид → Темы → Добавить новую
```

## 🐛 Устранение проблем

| Проблема | Решение |
|----------|---------|
| Ошибка подключения к БД | Проверьте, запущен ли MySQL, данные в wp-config.php |
| Белый экран | Включите WP_DEBUG, проверьте логи |
| 404 на страницах | Пересохраните постоянные ссылки |
| Не загружаются файлы | Проверьте права на wp-content/uploads, лимиты PHP |

## 📝 Автор

**Дуплей Максим Игоревич**

## 📄 Лицензия

WordPress распространяется под лицензией GPL v2 или новее.  
См. файл [license.txt](license.txt) для подробностей.

---

**Среда разработки** | WordPress 6.9.1 | PHP 8.4 | MariaDB
